// Create Event
box_width = 40;
box_height = 18;

// Center the box horizontally and slightly below the vertical center of the room
x1 = (room_width - box_width) / 2;  // Center horizontally
y1 = (room_height - box_height) / 2 + 60;  // Center vertically and move 10 pixels down

x2 = x1 + box_width;
y2 = y1 + box_height;
